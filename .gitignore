# Node / Next.js (backend/)
node_modules/
.next/
out/
*.tsbuildinfo
coverage/
next-env.d.ts

# Environment (never commit real values; .env.example is committed)
.env
.env.*
!.env.example

# Android (android/)
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/*.iml
android/.idea/
android/captures/
*.apk
*.aab
*.keystore
*.jks
# ...except the debug key, which is deliberately fixed so every build
# installs over the last one. It signs debug builds only, Play rejects it,
# and the password is Android's own public one. The release key is a
# different key and is not in this repository.
!android/app/debug.keystore
# Firebase's Android client config is not a secret — Google publishes it as a
# file that ships inside every APK, and the key in it is restricted to this
# package name plus the signing certificate. CI cannot build an FCM-enabled
# build without it, and this repository is private, so the app's copy is
# tracked. Any other copy that turns up stays ignored.
google-services.json
!android/app/google-services.json

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Logs
*.log
